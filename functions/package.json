{
  "name": "owez-functions",
  "version": "0.0.0",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions,firestore,auth,storage",
    "shell": "npm run build && firebase functions:shell",
    "verify": "tsx scripts/verify-phase1.ts"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0",
    "form-data": "^4.0.5",
    "mindee": "^5.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "firebase": "^11.0.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  }
}
